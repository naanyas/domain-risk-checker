streamlit>=1.28.0
pandas>=1.5.0
requests>=2.28.0
dnspython>=2.4.0
python-whois>=0.8.0
duckduckgo_search>=7.0.0
# v8.5: headless render. Pinned to match the Dockerfile's Playwright image
# (mcr…/playwright/python:v1.60.0) so the pip package lines up with the browser
# binaries baked into /ms-playwright. Installed into the same env as streamlit.
playwright==1.60.0
